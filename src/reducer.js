const initialState = {
};

const ActionType = {
};

const ActionCreator = {
};

const reducer = (state = initialState, action) => {
  switch (action.type) {
  }

  return state;
};


export {reducer, ActionType, ActionCreator};
